@page "/cart"
@rendermode InteractiveServer
@inject CartService CartService
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<PageTitle>Cart</PageTitle>

<h1 class="cart-title" style="padding: 20px;">Your Cart</h1>

@if (CartService.Items.Count == 0)
{
    <p class="empty-text">Your cart is empty.</p>
}
else
{
    <div class="cart-container">

        @foreach (CartItem item in CartService.Items)
        {
            <div class="cart-item">
                <img src="@item.Image" class="cart-img" />

                <div class="cart-info">
                    <h3>@item.Name</h3>
                    <p>Quantity: @item.Quantity</p>
                </div>

                <button class="add-btn" @onclick="() => DeleteItem(item)"><i class="fa fa-trash"></i></button>

                <span>Price: ₱@item.Price</span>
                
            </div>
        }
        <div class="total-price">
            <h2>Total: ₱@TotalPrice</h2>
        </div>
        <div class="payment-method">
        <h3>Payment method: </h3>
            <input type="radio" id="cash" name="payment-cash" value="HTML">
        <label for="payment">Cash</label><br>
        </div>
        
        <button class="checkout-btn">Place Order</button>
    </div>
}

@code {
    decimal TotalPrice =>
    CartService.Items.Sum(item => item.Price * item.Quantity);
    void DeleteItem(CartItem item)
    {
        CartService.Items.Remove(item);
    }
}
